---
import HeaderLink from './HeaderLink.astro';
import { SITE_TITLE } from '../consts';
---

<header class="header-container" id="main-header">
	<nav class="content-container">
		<div class="side-column">
			<a href="/" class="site-title">{SITE_TITLE}</a>
		</div>
		<div class="main-column">
			<div class="nav-links">
				<HeaderLink href="/blog">ARCHIVE</HeaderLink>
				<HeaderLink href="/about">INFO</HeaderLink>
			</div>
		</div>
	</nav>
</header>

<style>
	.header-container {
		position: sticky;
		top: 0;
		z-index: 100;
		background: var(--bg-primary);
		transition: transform 0.3s ease;
	}
	.content-container {
		padding-top: var(--space-8);
		padding-bottom: var(--space-8);
	}
	.site-title {
		font-weight: 700;
		font-size: 0.75rem;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		white-space: nowrap;
	}
	.nav-links {
		display: flex;
		gap: 3rem;
	}
	.nav-links :global(a) {
		font-family: ui-monospace, monospace;
		font-size: 0.65rem;
		letter-spacing: 0.1em;
		color: var(--text-muted);
		transition: color 0.2s ease;
	}
	.nav-links :global(a:hover),
	.nav-links :global(a.active) {
		color: var(--text-primary);
	}

	@media (max-width: 600px) {
		.nav-links {
			gap: 1rem;
		}
		.nav-links :global(a) {
			padding: 0.75rem 0.5rem; /* Increased tap target */
			margin: -0.75rem -0.5rem; /* Offset for alignment */
		}
		.content-container {
			padding-top: var(--space-4);
			padding-bottom: var(--space-4);
		}
	}
</style>
